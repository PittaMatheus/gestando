1. Erro de versão node
  error nome-do-meu-app@1.0.0: The engine "node" is incompatible with this module. Expected version "12.x". Got "10.16.3"

2. Instalando NVM(node version manager)
  curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash

3. instalando versão 12* node e alterando current
  nvm install 12.*
  nvm use v12.22.6

4. Adicionando lib de rotas
  yarn add react-router-dom
  yarn add @types/react-router-dom -D // Apenas ambiente de desenvolvimento

5 Adicionando lib styled components
  yarn add styled-components
  yarn add @types/styled-components -D  // Apenas ambiente de desenvolvimento

6 Adicionando lib react-switch
  yarn add react-switch

7 Adicionando lib react icons
  yarn add react-icons

8. Adicionando lib axios
  yarn add axios